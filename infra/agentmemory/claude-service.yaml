apiVersion: v1
kind: Service
metadata:
  name: agentmemory-claude
  namespace: agentmemory
spec:
  selector:
    app: agentmemory-claude
  type: LoadBalancer
  ports:
    - name: rest
      port: 3111
      targetPort: 3111
      protocol: TCP
