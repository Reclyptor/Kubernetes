apiVersion: v1
kind: Service
metadata:
  name: agentmemory-qwen
  namespace: agentmemory
spec:
  selector:
    app: agentmemory-qwen
  type: LoadBalancer
  ports:
    - name: rest
      port: 3211
      targetPort: 3111
      protocol: TCP
