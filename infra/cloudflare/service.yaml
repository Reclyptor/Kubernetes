apiVersion: v1
kind: Service
metadata:
  name: cloudflare-metrics
  namespace: cloudflare
spec:
  type: ClusterIP
  selector:
    app: cloudflare
  ports:
    - name: metrics
      port: 2000
      protocol: TCP

