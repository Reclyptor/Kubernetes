apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: all-drives
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: iscsi-persistent-fs6712x
  resources:
    requests:
      storage: 1Gi
