apiVersion: image.toolkit.fluxcd.io/v1
kind: ImageRepository
metadata:
  name: seraui
  namespace: seraui
spec:
  image: ghcr.io/reclyptor/seraui
  interval: 10m0s
  secretRef:
    name: ghcr-secret
---
apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: seraui
  namespace: seraui
spec:
  imageRepositoryRef:
    name: seraui
  filterTags:
    pattern: '^\d+$'
  policy:
    numerical:
      order: desc
---
apiVersion: image.toolkit.fluxcd.io/v1
kind: ImageUpdateAutomation
metadata:
  name: seraui
  namespace: seraui
spec:
  interval: 10m0s
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
  git:
    checkout:
      ref:
        branch: master
    commit:
      author:
        email: 5952751+Reclyptor@users.noreply.github.com
        name: Reclyptor
      messageTemplate: '{{range .Changed.Changes}}{{print .OldValue}} -> {{println .NewValue}}{{end}}'
    push:
      branch: master
  update:
    path: ./apps/seraui
    strategy: Setters
