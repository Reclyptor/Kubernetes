apiVersion: v1
kind: Service
metadata:
  name: moltbot
  namespace: moltbot
  labels:
    app: moltbot
spec:
  type: LoadBalancer
  selector:
    app: moltbot
  ports:
    - name: http
      port: 18789
      targetPort: 18789
      protocol: TCP
