apiVersion: v1
kind: Service
metadata:
  name: factorio-server
  namespace: factorio
spec:
  type: LoadBalancer
  selector:
    app: factorio
  ports:
    - name: game
      port: 34197
      protocol: UDP
    - name: rcon
      port: 27015
      protocol: TCP
