# Archives live on fs6712x, next to the game data.
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: dragonwilds-backups
  namespace: dragonwilds
spec:
  accessModes: ["ReadWriteOnce"]
  storageClassName: iscsi-persistent-fs6712x
  resources:
    requests:
      storage: 10Gi
