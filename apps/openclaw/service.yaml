apiVersion: v1
kind: Service
metadata:
  name: openclaw
  namespace: openclaw
  labels:
    app: openclaw
spec:
  type: LoadBalancer
  selector:
    app: openclaw
  ports:
    - name: http
      port: 18791
      targetPort: 18791
      protocol: TCP
