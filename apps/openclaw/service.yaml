apiVersion: v1
kind: Service
metadata:
  name: openclaw
  namespace: openclaw
spec:
  clusterIP: None
  selector:
    app: openclaw
  ports:
    - name: http
      port: 18791
      protocol: TCP
---
apiVersion: v1
kind: Service
metadata:
  name: belugabot
  namespace: openclaw
spec:
  clusterIP: None
  selector:
    app: belugabot
  ports:
    - name: http
      port: 18792
      protocol: TCP
